---
# yamllint disable-line rule:truthy
on: push
name: TOC Generator
jobs:
  generateTOC:
    name: Generate TOC
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4.1.5
        with:
          TOC_TITLE: 'Details'
          FOLDING: true
