sensor:

  - platform: history_stats
    name: Quarantine meter Person1
    entity_id: person.person1
    state: "home"
    type: ratio
    duration:
      days: 5
    end: "{{ now() }}"

  - platform: history_stats
    name: Quarantine meter Person1 times left
    entity_id: person.person1
    state: "home"
    type: count
    duration:
      days: 5
    end: "{{ now() }}"
