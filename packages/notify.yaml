---
notify:
  - name: pushover
    platform: pushover
    api_key: !secret pushover_api_key
    user_key: !secret pushover_user_key

  - name: admin
    platform: group
    services:
      - service: mobile_app_pontius

  - name: all
    platform: group
    services:
      - service: admin
      - service: robert
      - service: living_tv
