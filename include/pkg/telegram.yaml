telegram_bot:
  - platform: polling
    api_key: !secret telegram_bot_api_key
    allowed_chat_ids:
      - !secret telegram_bot_chat_id_robert

notify:
  - name: telegram_robert
    platform: telegram
    chat_id: !secret telegram_bot_chat_id_robert
