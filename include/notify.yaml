# notify:
- name: pushover
  platform: pushover
  api_key: !secret pushover_api_key
  user_key: !secret pushover_user_key

- name: person1
  platform: group
  services:
    - service: person1_mobile

- name: admin
  platform: group
  services:
    - service: pushover

- name: all
  platform: group
  services:
    - service: admin
    - service: person1
    - service: living_tv
