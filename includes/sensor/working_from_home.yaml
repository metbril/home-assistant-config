---
- platform: history_stats
  name: Working From Home Robert Today
  entity_id: binary_sensor.working_from_home_robert
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0) }}"
  end: "{{ now() }}"

- platform: history_stats
  name: Working From Home Monique Today
  entity_id: binary_sensor.working_from_home_monique
  state: "on"
  type: time
  start: "{{ now().replace(hour=0, minute=0, second=0) }}"
  end: "{{ now() }}"
