# How many calls has user had today
#
platform: history_stats
name: Teams Call Count Today Robert
entity_id: binary_sensor.robert_in_call
state: "on"
type: count
start: "{{ now().replace(hour=0, minute=0, second=0) }}"
end: "{{ now() }}"