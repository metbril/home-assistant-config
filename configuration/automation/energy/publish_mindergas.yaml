# publish first meter reading change after midinight to mindergas
alias: Publish Mindergas.nl
hide_entity: no # default no
trigger:
  platform: state
  entity_id: sensor.gas_consumption
#condition:
#  after: "00:00:00"
#  before: "01:00:00"
action:
  service: notify.pushover
  data_template:
    message: "Gasverbruik is nu {{ states.sensor.gas_consumption.state }} m3"
