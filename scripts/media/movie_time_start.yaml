movie_time_start:
  alias: Start Movie Time
  icon: mdi:movie-open
  mode: single

  sequence:
    - if:
        - condition: state
          entity_id: light.living
          state: 'on'
      then:
        - service: scene.turn_on
          target:
            entity_id: scene.movie_time
