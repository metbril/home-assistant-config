movie_time_stop:
  alias: Stop Movie Time
  icon: mdi:movie-open-off
  mode: single

  sequence:
    - if:
        - condition: state
          entity_id: light.living
          state: 'on'
      then:
        - service: scene.turn_on
          target:
            entity_id: scene.lighting_living_base
